// Tailwind v4 is a PostCSS plugin on its own; the v3 `tailwindcss` plugin entry
// must not be used here.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
