# Build output
build/
DerivedData/
*.xcarchive
*.dmg
*.zip

# Xcode user state
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# SwiftPM
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Explicit-module-build cache (produced by -target builds)
-Xcc/

# Editor / scanner state
.vscode/

# Website (website/) build output
website/node_modules/
website/.next/
website/out/
website/next-env.d.ts
website/*.tsbuildinfo
# bun keeps no store inside the repository, so bun.lock is committed and nothing else
# needs ignoring here.
